<UserControl x:Class="ITAM.WPF.Views.RoleManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="12">
        <DataGrid ItemsSource="{Binding Roles}"
                  SelectedItem="{Binding SelectedRole}">
            <DataGrid.Columns>
      
                <DataGridTextColumn Header="Mã vai trò" Binding="{Binding Code}" Width="120"/>
                <DataGridTextColumn Header="Tên vai trò" Binding="{Binding Name}" Width="250"/>
                <DataGridTextColumn Header="Số quyền" Binding="{Binding FeatureCount}" Width="100"/>
                <DataGridTextColumn Header="Số người dùng" Binding="{Binding UserCount}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
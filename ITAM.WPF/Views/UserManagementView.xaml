<UserControl x:Class="ITAM.WPF.Views.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="12">
        <DataGrid ItemsSource="{Binding Users}"
          SelectedItem="{Binding SelectedUser}"
          AutoGenerateColumns="False"
          IsReadOnly="True"
          CanUserAddRows="False"
          materialDesign:DataGridAssist.CellPadding="8"
          materialDesign:DataGridAssist.ColumnHeaderPadding="8">
            <DataGrid.Resources>
                <ContextMenu x:Key="UserRowContextMenu">
                    <MenuItem Header="Reset password"
                      Icon="{materialDesign:PackIcon Kind=LockReset}"
                      Command="{Binding DataContext.ResetPasswordCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                      CommandParameter="{Binding}"/>
                </ContextMenu>
            </DataGrid.Resources>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="ContextMenu" Value="{StaticResource UserRowContextMenu}"/>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tên đăng nhập" Binding="{Binding Username}" Width="150"/>
                <DataGridTextColumn Header="Họ tên" Binding="{Binding FullName}" Width="200"/>
                <DataGridTextColumn Header="Phòng ban" Binding="{Binding PhongBanName}" Width="150"/>
                <DataGridTextColumn Header="Vai trò" Binding="{Binding RoleNames}" Width="220"/>
                <DataGridCheckBoxColumn Header="Hoạt động" Binding="{Binding IsActive}" Width="90"/>
                <DataGridTextColumn Header="Đăng nhập gần nhất" Binding="{Binding LastLoginAt}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
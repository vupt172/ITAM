<Window x:Class="ITAM.WPF.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Đăng nhập - ITAM"
        Height="480" Width="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Window.InputBindings>
        <KeyBinding Key="Return"
                    Command="{Binding LoginCommand}"
                    CommandParameter="{Binding ElementName=PwdPassword}"/>
    </Window.InputBindings>

    <materialDesign:Card Margin="24" Padding="24" UniformCornerRadius="8">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="ĐĂNG NHẬP HỆ THỐNG ITAM"
                       Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,24"/>

            <TextBox x:Name="TxtUsername"
                     materialDesign:HintAssist.Hint="Tên đăng nhập"
                     materialDesign:TextFieldAssist.HasClearButton="True"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,16"/>

            <PasswordBox x:Name="PwdPassword"
                         materialDesign:HintAssist.Hint="Mật khẩu"
                         materialDesign:TextFieldAssist.HasClearButton="True"
                         Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                         Margin="0,0,0,8"/>

            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="{DynamicResource MaterialDesignValidationErrorBrush}"
                       TextWrapping="Wrap"
                       Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}"
                       Margin="0,4,0,8"/>

            <Button Content="ĐĂNG NHẬP"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Command="{Binding LoginCommand}"
                    CommandParameter="{Binding ElementName=PwdPassword}"
                    Height="40" Margin="0,12,0,0"/>

            <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                         IsIndeterminate="True"
                         Width="24" Height="24"
                         HorizontalAlignment="Center"
                         Margin="0,16,0,0"
                         Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </StackPanel>
    </materialDesign:Card>
</Window>
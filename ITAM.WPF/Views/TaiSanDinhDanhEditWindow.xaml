<Window x:Class="ITAM.WPF.Views.TaiSanDinhDanhEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Style="{StaticResource MaterialDesignWindow}"
        Title="Sửa Tài Sản Định Danh"
        Height="550" Width="460"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Window.InputBindings>
        <KeyBinding Key="Return" Command="{Binding SaveCommand}" />
        <KeyBinding Key="Escape" Command="{Binding CancelCommand}" />
    </Window.InputBindings>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBox Text="{Binding Code, Mode=OneWay}" IsReadOnly="True"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     materialDesign:HintAssist.Hint="Mã tài sản" Margin="0,0,0,16"/>
            <TextBox Text="{Binding TenHangHoa, Mode=OneWay}" IsReadOnly="True"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     materialDesign:HintAssist.Hint="Hàng hóa" Margin="0,0,0,16"/>
            <TextBox Text="{Binding TenLoaiTaiSan, Mode=OneWay}" IsReadOnly="True"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     materialDesign:HintAssist.Hint="Loại tài sản" Margin="0,0,0,16"/>
            <TextBox Text="{Binding TrangThai, Mode=OneWay}" IsReadOnly="True"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     materialDesign:HintAssist.Hint="Trạng thái" Margin="0,0,0,16"/>
            <TextBox Text="{Binding TenViTri, Mode=OneWay}" IsReadOnly="True"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     materialDesign:HintAssist.Hint="Vị trí hiện tại" Margin="0,0,0,16"/>
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     materialDesign:HintAssist.Hint="Tên tài sản" Margin="0,0,0,16"/>
            <TextBox Text="{Binding Serial, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     materialDesign:HintAssist.Hint="Serial" Margin="0,0,0,16"/>
            <TextBox Text="{Binding NamSuDung, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     materialDesign:HintAssist.Hint="Năm sử dụng" Margin="0,0,0,16"/>
            <TextBox Text="{Binding GhiChu, UpdateSourceTrigger=PropertyChanged}"
          Style="{StaticResource MaterialDesignFloatingHintTextBox}"
          materialDesign:HintAssist.Hint="Ghi Chú"/>
            
            <TextBlock Text="Đang tải dữ liệu..."
                       Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                       Foreground="Gray" Margin="0,8,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Lưu" Command="{Binding SaveCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,8,0"/>
            <Button Content="Hủy" Command="{Binding CancelCommand}"
                    Style="{StaticResource MaterialDesignOutlinedButton}"/>
        </StackPanel>
    </Grid>
</Window>
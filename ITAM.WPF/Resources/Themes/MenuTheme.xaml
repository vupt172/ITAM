<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <!-- COLORS -->
    
    <Color x:Key="MenuBackgroundColor">#CCD5F0</Color>
    <Color x:Key="MenuPopupBackgroundColor">#E9EEFF</Color>
    <Color x:Key="MenuForegroundColor">#1E1E1E</Color>
    <Color x:Key="MenuHoverColor">#B1C5FF</Color>
    <Color x:Key="MenuActiveColor">#E6EBF5</Color>
    <Color x:Key="MenuDisabledColor">#A2A2A2</Color>
    <Color x:Key="SeparatorColor">#A6B2D1</Color>

    <!-- BRUSHES -->
    <SolidColorBrush x:Key="MenuBackgroundBrush" Color="{StaticResource MenuBackgroundColor}" />
    <SolidColorBrush x:Key="MenuPopupBackgroundBrush" Color="{StaticResource MenuPopupBackgroundColor}" />
    <SolidColorBrush x:Key="MenuForegroundBrush" Color="{StaticResource MenuForegroundColor}" />
    <SolidColorBrush x:Key="MenuHoverBrush" Color="{StaticResource MenuHoverColor}" />
    <SolidColorBrush x:Key="MenuActiveBrush" Color="{StaticResource MenuActiveColor}" />
    <SolidColorBrush x:Key="MenuDisabledBrush" Color="{StaticResource MenuDisabledColor}" />
    <SolidColorBrush x:Key="SeparatorBrush" Color="{StaticResource SeparatorColor}" />


    <!-- MENU & MENU ITEM -->
    <LinearGradientBrush x:Key="MenubarBorderGradient" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#FFFFFF" Offset="0.0" />
        <GradientStop Color="#A6B2D1" Offset="1.0" />
    </LinearGradientBrush>
    <Style TargetType="Menu">
        <Setter Property="Background" Value="{StaticResource MenuBackgroundBrush}" />
        <Setter Property="Foreground" Value="{StaticResource MenuForegroundBrush}" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Padding" Value="5" />
        <Setter Property="BorderThickness" Value="0,1,0,1"/>
        <Setter Property="BorderBrush" Value="{StaticResource MenubarBorderGradient}"/>
    </Style>

    <Style TargetType="Separator">
        <Setter Property="Background" Value="{StaticResource SeparatorBrush}"/>
        <Setter Property="Margin" Value="28,3,2,3"/>
        <Setter Property="Height" Value="1"/>
    </Style>

    <Style TargetType="MenuItem">

        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource MenuForegroundBrush}" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Margin" Value="0" />



        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border x:Name="BgBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="Transparent"
                            BorderThickness="1"
                            CornerRadius="0"
                            SnapsToDevicePixels="True">

                        <Grid x:Name="GridContainer" MinHeight="22">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="28" SharedSizeGroup="IconGroup"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="GestureGroup"/>
                                <ColumnDefinition Width="15"/>
                            </Grid.ColumnDefinitions>

                            <ContentPresenter x:Name="IconPresenter" 
                                              ContentSource="Icon" 
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>

                            <ContentPresenter x:Name="HeaderPresenter"
                                              Grid.Column="1"
                                              ContentSource="Header"
                                              RecognizesAccessKey="True"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"/>

                            <TextBlock x:Name="InputGestureText"
                                       Grid.Column="2"
                                       Text="{TemplateBinding InputGestureText}"
                                       Margin="15,0,5,0"
                                       VerticalAlignment="Center"
                                       Foreground="{TemplateBinding Foreground}"
                                       HorizontalAlignment="Right"
                                       Opacity="0.8"/>

                            <Path x:Name="RightArrow"
                                  Grid.Column="3"
                                  Data="M 0,0 L 4,4 L 0,8" 
                                  Stroke="{TemplateBinding Foreground}"
                                  StrokeThickness="1.2"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  Visibility="Collapsed"/>

                            <Popup x:Name="SubMenuPopup"
                                   IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   Placement="Right"
                                   Focusable="False"
                                   PopupAnimation="None">
                                <Border Background="{StaticResource MenuPopupBackgroundBrush}"
                                        BorderBrush="{StaticResource SeparatorBrush}"
                                        BorderThickness="0"
                                        Padding="1"
                                        CornerRadius="0">
                                    <ScrollViewer x:Name="SubMenuScrollViewer" Style="{DynamicResource {x:Static GridView.GridViewScrollViewerStyleKey}}">
                                        <Grid RenderOptions.ClearTypeHint="Enabled">
                                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle"/>
                                        </Grid>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="BgBorder" Property="Background" Value="{StaticResource MenuHoverBrush}" />
                            <Setter TargetName="BgBorder" Property="BorderBrush" Value="#A6B2D1" />
                        </Trigger>

                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="Role" Value="TopLevelHeader"/>
                                <Condition Property="IsSubmenuOpen" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="BgBorder" Property="Background" Value="{StaticResource MenuActiveBrush}" />
                            <Setter TargetName="BgBorder" Property="BorderBrush" Value="#8EA3D3" />
                        </MultiTrigger>

                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="Role" Value="SubmenuHeader"/>
                                <Condition Property="IsSubmenuOpen" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="BgBorder" Property="Background" Value="{StaticResource MenuHoverBrush}" />
                            <Setter TargetName="BgBorder" Property="BorderBrush" Value="#A6B2D1" />
                        </MultiTrigger>

                        <Trigger Property="Role" Value="TopLevelHeader">
                            <Setter TargetName="SubMenuPopup" Property="Placement" Value="Bottom"/>
                            <Setter TargetName="RightArrow" Property="Visibility" Value="Collapsed"/>
                        </Trigger>

                        <Trigger Property="Role" Value="SubmenuHeader">
                            <Setter TargetName="SubMenuPopup" Property="Placement" Value="Right"/>
                            <Setter TargetName="RightArrow" Property="Visibility" Value="Visible"/>
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource MenuDisabledBrush}"/>
                            <Setter TargetName="InputGestureText" Property="Foreground" Value="{StaticResource MenuDisabledBrush}"/>
                            <Setter TargetName="RightArrow" Property="Stroke" Value="{StaticResource MenuDisabledBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

    </Style>

    <!-- TOOLBAR -->
    
</ResourceDictionary>